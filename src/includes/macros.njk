{% macro video(src, muted = true) %}<video src="{{ src }}" controls {{ "muted" if muted }} playsinline preload="metadata"></video>{% endmacro %}

{% macro img(src, alt = '') %}<img src="{{ src }}" alt="{{ alt }}" loading="lazy" deconding="async">{% endmacro %}
